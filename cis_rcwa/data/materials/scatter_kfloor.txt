# 안료 산란 바닥 (물질의 성질) — 원본 n,k 파일은 무수정
#
# 평막 투과 n,k 측정은 안료 입자 산란광이 검출기로 들어가 '투과'로 집계되어
# 산란 소광을 원리적으로 못 잡는다. 소자(0.7um 픽셀)에서는 그 산란이 옆
# 픽셀 손실이 된다. 로딩 시 k = max(측정 k, 바닥) 으로 적용된다
# (src/materials/library.py).
#
# 검증: 4개 파장(520/540/550/560)에서 독립 역산한 필요 k 가 0.0100~0.0112
# 한 점에 수렴 (+-7%). 쿠폰 투과 peak ~90% 실측과 현재 n,k 는 일치 —
# n,k 데이터는 옳고 빠진 것은 산란뿐.
#
# green 만 등재하는 이유: blue 통과대역 k(0.014)는 바닥보다 높아 무영향,
# red 는 장파장에서 시뮬<실측이라 바닥 넣을 근거 없음 (안료가 다르면 산란도
# 다름). ** 적분구(haze) 실측이 오면 이 값을 교체할 것 (색마다 1회). **
#
# 형식: 물질이름  k_floor
cf_green   0.0105
